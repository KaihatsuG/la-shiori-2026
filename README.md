# la-shiori-2026

家族旅行のしおり（個人用）。

このリポジトリの `payload.json` は **AES-256-GCM で暗号化**されており、
合い言葉を知らない限り内容を読むことはできません。

- 鍵導出: PBKDF2-HMAC-SHA256 / 310,000 回
- 暗号化: AES-256-GCM
- 復号はすべて閲覧者の端末内で行われます（サーバー側に鍵はありません）

Service Worker により、一度開けばオフラインで動作します。
